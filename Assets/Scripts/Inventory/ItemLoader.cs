using UnityEngine;
using Inventory;

public class ItemLoader : MonoBehaviour {

    //public const string path = "items";

    //public static InventoryItem GetInventoryItem(string itemName)
    //{
    //    ItemContainer ic = ItemContainer.Load(path);

    //    for (int i = 0; i < ic.weapons.Count; i++)
    //    {
    //        if (ic.weapons[i].ItemName == itemName)
    //            return ic.weapons[i];
    //    }

    //    for (int i = 0; i < ic.consumableItems.Count; i++)
    //    {
    //        if (ic.consumableItems[i].ItemName == itemName)
    //            return ic.consumableItems[i];
    //    }

    //    return null;
    //}
}